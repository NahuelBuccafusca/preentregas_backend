<div>
  {{#if user}}
    <a
      href="/carts/{{user.cart}}"
      id="imgCart"
    >
      {{#if cart.length}}
        <p >{{cart.length}}</p>
      {{else}}
        <p></p>
      {{/if}}
      shopping_cart
    </a>
  {{else}}
  {{/if}}
  <section >
    <h3>{{product.title}}</h3>
    <div >
      <div>
        <p>Descripción: {{product.description}}</p>
        <p>Precio: $ {{product.price}}.-</p>
        <p>Categoria: {{product.category}}</p>
        <div>
          {{#if user}}
            <form
              action="/carts/{{user.cart}}/products/{{product._id}}"
              method="POST"
            ><button>Agregar </button></form>
            <a href="/products"><button>Seguir comprando</button></a>
          {{else}}
            <a href="/products"><button>Ver todos</button></a>
          {{/if}}
        </div>

      </div>

    </div>
  </section>
</div>

<script src="/js/productDetails.js"></script>